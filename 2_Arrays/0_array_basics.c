#include <stdio.h>

int main()
{
    // int A[5];
    // A[0] = 12;
    // A[1] = 15;
    // A[2] = 25;
    int A[10] = {2,4};

    for (int i = 0; i < 10; i++)
    {
        printf("%d ", A[i]);
    }
    

    // printf("%d\n", sizeof(A));
    // printf("%d\n", A[1]);
    // printf("%d\n", A[8]);

    return 0;
}